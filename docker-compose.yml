version: '2'
services:
  web:
    image: typesafety/hackunit:latest
    build:
      context: .
      args:
        hack_user: develop
        hack_group: develop
    user: develop
    tty: true
    cap_add:
      - ALL
    cap_drop:
      - NET_ADMIN
      - SYS_ADMIN
